.item
  %blockquote{ lang:reference['lang'] }
    %p= reference['reference']
    %small
      = reference['type']
      %cite
        == #{reference['name']}, #{reference['position']}
        == (#{reference['given_at']})
